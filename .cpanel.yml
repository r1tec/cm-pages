---
# DESATIVADO de proposito. Este deploy copiava coe/index.html CRU (sem otimizar,
# sem limpar cache) e so funcionava para a slug coe.
# O unico caminho de publicacao e  ./publicar.sh  (ver .claude/skills/publicar).
deployment:
  tasks:
    - /bin/echo "Publicacao por cPanel desativada. Use ./publicar.sh"
